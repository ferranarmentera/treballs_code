using System;

namespace DivisionPractices

{
    class DivisionPractices
    {
        static void Main(string[] args)
        {
            // user impùts two values : cylinder radius and cilinder height  to calculate the cylinder volume
            int a = 7;
            int b = 2;
            int result = a / b;

            
            Console.WriteLine(result);
        }
    }
}
