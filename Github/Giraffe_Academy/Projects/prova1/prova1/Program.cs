using System;

namespace prova1
{
    class Program
    {
        static void Main(string[] args)
        {
            //prova del funcionament de les variables assignades en un pas
            // short guarda petits integers en 2 bits 
            short LevelNumber = 4;
            int score = 0;
            /*variables assignades en dos passos
            short can be any value from -32768 to 32767, whereas ushort can be from 0 to 65535.
            They have the same total range and use the same number of bits but are interpreted in
            different ways, and have different maximums/minimums.*/



            long aBigNumber;
            aBigNumber = -1000;

            //Aquesta ocupara  un bite i pot ocupar entre0 i 255 caracters 


            Console.WriteLine("Hello World!");
        }
    }
}
